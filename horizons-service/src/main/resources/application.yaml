server:
  port: 8082

spring:
  application:
    name: HORIZONS-SERVICE
  config:
    import: optional:configserver:http://localhost:8087
# eureka:
#   instance:
#     prefer-ip-address: true
#   client:
#     fetch-registry: true
#     register-with-eureka: true
#     service-url:
#       defaultZone: http://localhost:8761/eureka
